name: '${pluginName}'
version: '${version}'
main: org.yuemi.mmoitems.plugin.MmoItemsPlugin
authors: [${authors}]
api-version: '${apiVersion}'

depend: [
  YueMiLibs
]

softdepend: [
  PlaceholderAPI
]

commands:
  mmoitems:
    description: "Main command for MC-MMO-Items"
    aliases: [items]
    permission: mmoitems.admin

